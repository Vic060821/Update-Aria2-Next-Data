The source of trackerlists
> [ngosang/trackerslist](https://ngosang.github.io/trackerslist/)

> [XIU2/TrackersListCollection](https://github.com/XIU2/TrackersListCollection)

The source of ED2K nodes and servers
> [eMule-Security.org](https://emule-security.org/)

By default it will update the content of "bt-trackers" flag in `aria2.conf` that placed next to the script

The `server.met` and `nodes.dat` will replace the old ones next to the script
